<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Hair Salon</title>
    </head>
    <body>
        <h1>Hair Salon</h1>
            <h2>Step 1 for new clients: Enter your information</h2>
            <form action="/client" method="post">
                <label for="name">Client Name:</label>
                <input id="name" type="text" name="name" placeholder="Enter your name...">
                <label for="stylist">Pick a stylist</label>
                <select id="stylist" name='stylist'>
                    {% for stylist in stylists %}
                        <option value={{ stylist.getId }}>{{ stylist.getName }}</option>
                    {% endfor %}
                </select>
                <button type="submit" name="button">Complete.</button>
            </form>

            {% if stylists is not empty %}
               <h3>Available Stylists:</h3>
               <ul>
                   {% for stylist in stylists %}
                       <li><a href="/stylists/{{stylist.getId()}}">{{stylist.getName()}}</a></li>
                   {% endfor %}
               </ul>
               {% endif %}


            <h2>If you are already a client, check out these options!</h2>
            <p><a href='/clients'>See client list</a></p>
            <p><a href='/stylists'>See stylist list</a></p>
            <p><a href='/add_stylist'>Add a stylist</a></p>
    </body>
</html>
